
mutation CreateSessionByGoogleIdToken($idToken: String!) {
    session: createSessionByGoogleIdToken(idToken: $idToken) {
        token
        user {
            email
        }
    }
}

mutation UpdateGlucoseUnit($settingsInput: SettingsInput!) {
    settings: updateSettings(input: $settingsInput) {
        bloodGlucoseUnits
    }
}

mutation CreateInsulin($name: String!, $color: String!) {
    insulin: createInsulin(input: {name: $name, color: $color}) {
        id
        name
        color
    }
}

mutation UpdateInsulin($id: ID!, $name: String!, $color: String!) {
    insulin: updateInsulin(id: $id, input: {name: $name, color: $color}) {
        id
        name
        color
    }
}

mutation DeleteInsulin($id: ID!) {
    insulin: deleteInsulin(id: $id) {
        id
        name
        color
    }
}